# ============================================================
# SOTB Scripted Effects
# Shadows Over The Balkans — Reusable Effects
# ============================================================

# --- Phase 0: Foundation Effects ---

# Remove Delchev's watching spirit (triggers when succession resolves)
SOTB_remove_delchev_watches = {
	remove_ideas = {
		MAC_founder_watches
	}
}

# Goce Delchev returns as country leader
SOTB_delchev_returns = {
	create_country_leader = {
		name = MAC_delchev
		desc = MAC_delchev_desc
		picture = GFX_portrait_MAC_goce_delchev
		ideology = populism
		traits = { delchevism_founder }
		expire = "1965.1.1"
	}
	remove_ideas = {
		MAC_founder_watches
		MAC_lion_watches
	}
	add_ideas = {
		MAC_delchev_returns
	}
}

# Jane Sandanski takes the Lion's Path
SOTB_sandanski_path = {
	create_country_leader = {
		name = MAC_sandanski
		desc = MAC_sandanski_desc
		picture = GFX_portrait_MAC_jane_sandanski
		ideology = conservatism
		traits = { lion_of_solun }
		expire = "1965.1.1"
	}
	remove_ideas = {
		MAC_founder_watches
		MAC_lion_watches
	}
	add_ideas = {
		MAC_lion_leads
	}
}

# Todor Aleksandrov seizes power (Non-Aligned military)
SOTB_aleksandrov_takes_power = {
	create_country_leader = {
		name = MAC_aleksandrov
		desc = MAC_aleksandrov_desc
		picture = GFX_portrait_MAC_todor_aleksandrov
		ideology = despotism
		traits = { pragmatic_soldier }
		expire = "1965.1.1"
	}
	remove_ideas = {
		MAC_founder_watches
		MAC_lion_watches
		MAC_delcevism
	}
	add_ideas = {
		MAC_aleksandrov_order
	}
}

# Ivan Mihailov's fascist takeover
SOTB_mihailov_coup = {
	create_country_leader = {
		name = MAC_mihailov
		desc = MAC_mihailov_desc
		picture = GFX_portrait_MAC_ivan_mihailov
		ideology = fascism_ideology
		traits = { the_avenger }
		expire = "1965.1.1"
	}
	remove_ideas = {
		MAC_founder_watches
		MAC_lion_watches
		MAC_delcevism
		MAC_throne_of_ohrid
	}
	add_ideas = {
		MAC_iron_guard
	}
}

# Resolve the Old Guard vs Reform debate (called by military tree)
SOTB_resolve_old_guard = {
	remove_ideas = {
		MAC_old_guard_reform
	}
}

# --- Phase 3: Resource Addition Effects ---

# Add steel to state 106 (Kratovo-Zletovo mining district)
SOTB_add_kratovo_steel = {
	106 = {
		add_resource = {
			type = steel
			amount = 8
		}
	}
}

# Add chromium to state 106 (Raduša deposit near Skopje)
SOTB_add_radusa_chromium = {
	106 = {
		add_resource = {
			type = chromium
			amount = 6
		}
	}
}

# Add coal to state 970 (Pelagonian lignite basin near Bitola)
SOTB_add_bitola_coal = {
	970 = {
		add_resource = {
			type = coal
			amount = 8
		}
	}
}

# Add oil to state 1083 (Aegean offshore — Prinos field near Kavala)
SOTB_add_prinos_oil = {
	1083 = {
		add_resource = {
			type = oil
			amount = 8
		}
	}
}

# --- Phase 3: Opium Effects (stubs for now) ---

# Set opium dependency stage for a target nation
# Usage: SOTB_set_opium_dependency = { target = GRE stage = 2 }
SOTB_set_opium_dependency = {
	set_variable = {
		var = SOTB_opium_dependency_$target$
		value = $stage$
	}
}

# Calculate total opium leverage (sum of all dependencies)
SOTB_calculate_opium_leverage = {
	# Stub — implemented in Phase 3
}

# --- Phase 4: Albania Effects (stubs for now) ---

SOTB_albania_submit_check = {
	# Stub — implemented in Phase 4
}
